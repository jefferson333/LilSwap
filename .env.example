# Environment variables for LilSwap frontend
#
# This file is intended to be the single example tracked in the repository
# and should contain the official production endpoints (placeholders).
# Contributors should copy this file to a local override (see below)
# when they need to run against a local engine or enable debug logging.
#
# Vite loads env files in this order (lowest → highest priority):
#   .env → .env.local → .env.[mode] → .env.[mode].local
#
# Production API (placeholder) - used in CI / public builds
VITE_API_URL=https://api.lilswap.xyz/v1

# Build / runtime mode (production by default for builds)
VITE_ENV=production

# Logging level (frontend). Change to 'debug' locally if needed.
VITE_LOG_LEVEL=error

# Local overrides (NOT committed):
# - Copy this file to `.env.local` and edit values for local development.
#   Example local overrides (in `.env.local`):
#     VITE_API_URL=http://localhost:3001/v1
#     VITE_LOG_LEVEL=debug

# Reown Project ID for AppKit / WalletConnect (get yours at cloud.reown.com)
VITE_REOWN_PROJECT_ID=REOWN_PROJECT_ID_HERE